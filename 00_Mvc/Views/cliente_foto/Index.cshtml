@model IEnumerable<_04_Data.Data.cliente_foto>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.fecha_visionado)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.me_gusta)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cliente.login)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.foto.descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.fecha_visionado)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.me_gusta)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cliente.login)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.foto.descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
